apiVersion: v1
kind: Service
metadata:
  name: vps-mcp
  labels:
    app.kubernetes.io/name: vps-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vps-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
